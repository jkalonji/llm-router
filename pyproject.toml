[project]
name = "llm-router"
version = "0.1.0"
description = "Routage intelligent de prompts vers le modèle Groq le plus adapté"
requires-python = ">=3.11"
dependencies = [
    "groq>=0.9.0",
    "mcp>=1.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = []
